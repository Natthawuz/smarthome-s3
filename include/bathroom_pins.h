/*
  bathroom_pins.h — พินอุปกรณ์ห้องน้ำ (ปุ่ม SOS)
  -----------------------------------------------------------
  แก้ไขสำคัญ: ESP32-S3 ไม่มีขา GPIO22-25 อยู่จริงในตัวชิปเลย (เลขข้ามจาก 21 ไป 26
  โดยตรง ไม่ใช่บอร์ดตัดออก) ที่เคยจดไว้ว่า 22-25 ว่างจึงผิด — ยืนยันจากรูปบอร์ดจริง
  (V2775) ที่แถวขาซ้ายจบที่ 21 แล้วอีกฝั่งกระโดดไป 35-48

  ยิ่งไปกว่านั้น ขาที่มีอยู่จริงบนบอร์ดถูกจองครบแล้วจากทุกเฟสก่อนหน้า (กล้อง+servo
  ฝั่งซ้าย, MPU-6050+SD+buzzer+DHT22+OLED ฝั่งขวา) จึงต้อง "ใช้ซ้ำ" ขาจากเฟสอื่นที่
  ไม่ได้ใช้งานพร้อมกันจริง (แต่ละเฟสเป็นเฟิร์มแวร์แยกไฟล์ ไม่ได้รันพร้อมกันบนบอร์ด
  เดียว — ยังไม่ได้รวมเป็นเฟิร์มแวร์เดียว ดูหมายเหตุ "นอกขอบเขต" ในแผน fall detection)

  SOS_BUTTON_PIN ใช้ขาเดียวกับ servo ห้องโถง (phase2, IO14) เพราะเฟสห้องน้ำนี้ไม่ใช้
  servo เลย — BUZZER_PIN ใช้ขาเดียวกับ phase5d-fall-detection (IO2)
-----------------------------------------------------------
*/
#pragma once

#define SOS_BUTTON_PIN 14  // ขา S ของโมดูลปุ่ม SOS (มี GND/VCC/S) — ไม่ใช้ INPUT_PULLUP เพราะโมดูลมีวงจรของตัวเองแล้ว
#define BUZZER_PIN     2   // buzzer เดียวกับ phase5d-fall-detection
